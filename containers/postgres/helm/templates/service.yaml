apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
  - name: postgres
    protocol: TCP
    port: 5432
  selector:
    app: {{ .Release.Name }}
