Configuration for SUSE Manager. 

Asks user for data, saves them into environment variables and to environment file (/tmp/setup_env.sh) and runs /usr/lib/susemanager/bin/migration.sh to process the data.


Installation

    Copy ycp files under /usr/share/YaST2/clients

Possible ways to run:

1. firstboot

    Add the steps into your firstboot.xml file, see firstboot.xml.snippet.
    Last step must be susemanager_write.

2. standalone

    File susemanager_setup.ycp contains standalone workflow.
    Run 'yast2 susemanager_setup'


What is missing

     more data validation, error handling, help texts, translations
