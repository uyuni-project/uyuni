apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uyuni.fullname" . }}-test-connection"
  labels:
    {{- include "uyuni.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uyuni.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
