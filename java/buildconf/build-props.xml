<project name="build-props">
  <property name="rhn-home" location="." />

  <property file="${user.home}/.rhn.properties" />
  <property name="src.dir" location="${rhn-home}/code" />
  <property name="build.dir" location="${rhn-home}/build" />
  <property name="lib.dir" location="${rhn-home}/lib" />
  <property name="build.lib.dir" location="${build.dir}/build-lib" />

  <property name="run.lib.dir" location="${build.dir}/run-lib" />
  <property name="run-external.lib.dir" location="${build.dir}/run-lib/external" />
  <property name="test.lib.dir" location="${build.dir}/test-lib" />
  <property name="boot.lib.dir" location="${build.dir}/boot-lib" />
  <property name="report.dir" location="${build.dir}/reports" />
  <property name="taskomatic.link.dir" location="/usr/share/spacewalk/taskomatic" />

  <property name="log4j.threshold" value="warn" />
  <property name="halt-tests-on-failure" value="false" />
  <property name="checkstyle.header.file" value="${rhn-home}/buildconf/LICENSE.txt" />
  <property name="checkstyle.cache.src" value="${rhn-home}/build/checkstyle.cache.src" />
  <property name="checkstyle.cache.test" value="${rhn-home}/build/checkstyle.cache.test" />
  <property name="javadoc.lazy" value="false" />
  <property name="deprecation" value="off" />
  <property name="nowarn" value="!${deprecation}" />
  <property name="includehelp" value="true" />

  <!-- IVY Properties -->
  <property name="ivy.log.module.when.found" value="false"/>
  <property name="ivy.log.resolved.revision" value="false"/>
  <property name="ivy.dep.file" value="${rhn-home}/buildconf/ivy/ivy-suse.xml" />
  <property name="ivy.settings.file" value="${rhn-home}/buildconf/ivy/ivyconf.xml" />

  <!-- END IVY Properties -->

  <property name="rhn.war" value="${build.dir}/webapp/rhnjava.war"/>
  <property name="megajar.jar" value="${run.lib.dir}/rhn.jar" />
  <property name="testjar.jar" value="${run.lib.dir}/rhn-test.jar" />

  <!-- =================== jar dependencies ======================= -->
  <property name="commons-lang" value="commons-lang3"/>

  <condition property="byte-buddy" value="byte-buddy/byte-buddy.jar" else="byte-buddy.jar">
    <available file="${java.lib.dir}/byte-buddy/byte-buddy.jar"/>
  </condition>

  <condition property="commons-validator" value="apache-commons-validator" else="commons-validator">
    <available file="${java.lib.dir}/apache-commons-validator.jar" />
  </condition>

  <condition property="commons-compress" value="apache-commons-compress" else="commons-compress">
    <available file="${java.lib.dir}/apache-commons-compress.jar" />
  </condition>

  <property name="log4j-core" value="log4j/log4j-core" />
  <property name="log4j-api" value="log4j/log4j-api" />
  <property name="log4j-jcl" value="log4j/log4j-jcl" />
  <property name="log4j-slf4j-impl" value="log4j/log4j-slf4j-impl" />
  <property name="log4j-jars" value="${log4j-core} ${log4j-api} ${log4j-jcl} ${log4j-slf4j-impl}"/>

  <property name="jta" value="jakarta-transactions" />

  <property name="javamail" value="jakarta-mail/jakarta.mail-api angus-mail/jakarta.mail" />

  <property name="commons-jexl" value="apache-commons-jexl/commons-jexl" />

  <property name="jcache" value="jcache" />

  <property name="jaf" value="jakarta-activation/jakarta.activation-api angus-activation/angus-activation" />

  <property name="jaxb-api" value="jaxb-api/jakarta.xml.bind-api" />
  <property name="jaxb-impl" value="jaxb/jaxb-core jaxb/jaxb-runtime jaxb/txw2 istack-commons/istack-commons-runtime" />
  <property name="jaxb" value="${jaxb-api} ${jaxb-impl}"/>

  <property name="jstl" value="jakarta-jstl/jakarta.servlet.jsp.jstl-api jakarta-jstl/jakarta.servlet.jsp.jstl" />

  <property name="hibernate-core" value="hibernate-core"/>
  <property name="hibernate-models" value="hibernate-models/hibernate-models"/>

  <property name="jpa" value="jakarta-persistence-api/jakarta.persistence-api"/>

  <property name="ehcache" value="ehcache/ehcache-jakarta" />

  <property name="hibernatedeps"
            value="${jpa} ${hibernate-core} ${hibernate-models} slf4j/api ${byte-buddy} jboss-logging ${jcache}
              ${ehcache} classmate/classmate hypersistence-utils/hypersistence-utils-hibernate-71 jackson-databind
              jackson-core jackson-annotations"/>

  <property name="c3p0" value="mchange-commons/mchange-commons-java c3p0"/>

  <property name="postgresql-jdbc"
            value="postgresql-jdbc ongres-scram/client ongres-scram/common ongres-stringprep/saslprep
              ongres-stringprep/stringprep" />

  <property name="common.jar.dependencies"
            value="commons-beanutils commons-cli commons-codec commons-collections commons-digester commons-discovery
              commons-fileupload2-core commons-fileupload2-jakarta-servlet6 commons-io ${commons-lang}
              apache-commons-text/commons-text commons-logging ${commons-validator} ${hibernatedeps}
              ${commons-compress} ${javamail} smtp jdom ${log4j-jars} oro redstone-xmlrpc-client redstone-xmlrpc
              ${struts-jars} ${tomcat-jars} ${postgresql-jdbc} quartz slf4j-api
              velocity-engine-core simple-core" />

  <!-- deps needed for testing, but required to build -->
  <property name="test.build.jar.dependencies" value="junit ${log4j-jars} regexp postgresql-jdbc" />

  <property name="test.jar.dependencies"
            value="antlr4/antlr4-runtime checkstyle jdom ${jta} junit ${log4j-jars} regexp postgresql-jdbc" />

  <condition property="ant-contrib.path" value="ant/ant-contrib" else="ant-contrib">
    <available file="${java.lib.dir}/ant/ant-contrib.jar"/>
  </condition>

  <property name="build.jar.dependencies"
            value="ant ant-junit ${ant-contrib.path} antlr4/antlr4-runtime ${test.build.jar.dependencies}
              ${common.jar.dependencies} ${tomcat-jars} ${jasper-jars} xalan-j2" />

  <property name="run.jar.dependencies"
            value="antlr4/antlr4-runtime ${c3p0} commons-discovery ${jaf} ${jta} sitemesh
              xalan-j2 xalan-j2-serializer xerces-j2 ${common.jar.dependencies}" />

  <!-- =================== RPM build, use jpackage syntax ======================= -->

  <!-- SUSE extra dependencies: build and runtime -->
  <property name="suse-common-jars" value="jade4j jose4j salt-netapi-client spark-core spark-template-jade httpasyncclient simpleclient simpleclient_common simpleclient_hibernate simpleclient_servlet simpleclient_httpserver pgjdbc-ng/pgjdbc-ng pgjdbc-ng/spy netty/netty-common netty/netty-buffer netty/netty-resolver netty/netty-transport netty/netty-codec netty/netty-handler netty/netty-transport-native-unix-common java-saml java-saml-core joda-time woodstox-core-asl xmlsec stax2-api stax-api ${commons-jexl} ical4j ${product-common-jars} ${jaxb} ${jstl}" />

  <!-- SUSE extra dependencies: runtime only -->
  <property name="suse-runtime-jars" value="${commons-lang} apache-commons-text/commons-text concurrentlinkedhashmap-lru slf4j/api ${product-runtime-jars} jctools/jctools-core" />

  <property name="install.build.jar.dependencies"
            value="ant ant/ant-junit ${ant-contrib.path} antlr4/antlr4-runtime ${jasper-jars} ${test.build.jar.dependencies}
              ${install.common.jar.dependencies} xalan-j2" />

  <property name="install.run.jar.dependencies"
            value="antlr4/antlr4-runtime ${c3p0} commons-discovery ${jaf} ${jta} sitemesh
              xalan-j2 xalan-j2-serializer xerces-j2 ${install.common.jar.dependencies}" />

  <property name="install.common.jar.dependencies"
            value="commons-beanutils commons-cli commons-codec commons-collections commons-digester commons-discovery
              commons-fileupload2-core commons-fileupload2-jakarta-servlet6 commons-io ${commons-lang}
              apache-commons-text/commons-text commons-logging ${commons-validator} ${hibernatedeps} ${commons-compress}
              ${tomcat-jars} ${javamail} jdom jsch ${log4j-jars} oro redstone-xmlrpc-client redstone-xmlrpc
              ${struts-jars} ${postgresql-jdbc} quartz ${suse-common-jars} velocity-engine-core simple-core snakeyaml
              simpleframework/simplexml ${commons-jexl}" />

  <property name="dist.jar.dependencies"
            value="antlr4/antlr4-runtime ${c3p0} commons-collections commons-beanutils commons-cli commons-codec
              commons-digester commons-discovery commons-fileupload2-core commons-fileupload2-jakarta-servlet6
              commons-io ${commons-lang} apache-commons-text/commons-text commons-logging ${commons-compress}
              ${commons-validator} ${hibernatedeps} ${jta} ${jaf} ${jasper-jars} ${javamail} ${jaxb} ${jstl} jdom
              ${tomcat-jars} ${log4j-jars} redstone-xmlrpc-client redstone-xmlrpc oro quartz sitemesh ${struts-jars}
              ${postgresql-jdbc} snakeyaml simpleframework/simplexml ${suse-common-jars} ${suse-runtime-jars}
              xalan-j2 xalan-j2-serializer xerces-j2 simple-core ${jcache} ${other-jars}" />

  <property name="taskomatic.jar.dependencies" value="${dist.jar.dependencies} jsch" />

  <path id="bootjars">
    <fileset dir="${boot.lib.dir}" includes="*.jar" />
  </path>

  <path id="testjars">
    <fileset dir="${test.lib.dir}" includes="*.jar" />
  </path>

  <path id="runjars">
    <fileset dir="${run.lib.dir}" includes="**/*.jar" />
  </path>

  <path id="buildjars">
    <path refid="testjars" />
    <fileset dir="${build.lib.dir}">
      <include name="*.jar" />
    </fileset>
    <pathelement location="${build.dir}/classes/" />
  </path>

  <path id="alljars">
    <path refid="buildjars" />
    <path refid="runjars" />
  </path>

  <!-- =================== Deployment Properties ======================= -->
  <!-- Used for local dir deployment -->
  <property name="webapp.basedir"    value="${rhn-home}" />
  <property name="webapp.name"       value="rhnwebapp" />
  <property name="webapp.dir"        value="${webapp.basedir}/${webapp.name}" />

</project>
