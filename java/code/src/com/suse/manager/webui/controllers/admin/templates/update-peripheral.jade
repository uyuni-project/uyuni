include /templates/common.jade

#update-peripheral

script(type='text/javascript').
    window.csrfToken = "#{csrf_token}";

script(type='text/javascript').
    spaImportReactPage('admin/hub/peripheral/:id')
        .then(function (module) {
            module.renderer(
                'update-peripheral', !{peripheral}
            )
        });
