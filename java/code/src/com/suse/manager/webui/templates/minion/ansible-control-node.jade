include ./minion-header.jade

script(type='text/javascript').
    window.csrfToken = "#{csrf_token}";

#ansible-control-node
    script(type='text/javascript').
        injectReactPage('minion/ansible/control-node', {
            id: #{server.id}
        });
