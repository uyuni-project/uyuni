- name: SSH public key absence on given system
  hosts: all
  tasks:
    - name: Ensure the given ssh_pubkey not to be in the authorized keys for given user
      authorized_key:
        state: absent
        user: '{{user}}'
        key: '{{ssh_pubkey}}'
