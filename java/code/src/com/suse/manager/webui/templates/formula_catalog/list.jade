include ../common.jade

#formula-catalog
  script(type='text/javascript').
    injectReactPage('salt/formula-catalog/org-formula-catalog', {
      flashMessage: "#{info}",
      warningMessage: "#{warning}"
    });
