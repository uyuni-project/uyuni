include ../common.jade

#image-build

script(type='text/javascript').
    const csrfToken = "#{csrf_token}";
    const profileId = "#{profileId}";
    const hostId = "#{hostId}";
    const version = "#{version}";
    const timezone = "#{h.renderTimezone()}";
    const localTime = "#{h.renderLocalTime()}";
    const actionChains = !{actionChains};

script(src='/javascript/manager/images/image-build.bundle.js?cb=#{webVersion}', type='text/javascript')
