sudo: false
language: ruby

rvm:
  - 2.3.0
before_install: gem update --remote bundler
install:
  - bundle install --gemfile=Gemfile.checks --retry=3
script:
  - bundle exec rubocop
  - bundle exec cucumber_lint

