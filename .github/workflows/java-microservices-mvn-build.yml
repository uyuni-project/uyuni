name: Java Microservices Maven Build

on:
  push:
    branches:
      - master
    paths:
      - 'java/uyuni-java-parent/**'
      - 'java/uyuni-java-common/**'
      - 'java/coco-attestation/**'
      - '.github/workflows/java-microservices-mvn-build.yml'
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    paths:
      - 'java/uyuni-java-parent/**'
      - 'java/uyuni-java-common/**'
      - 'java/coco-attestation/**'
      - '.github/workflows/java-microservices-mvn-build.yml'


jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0
      - name: Set up JDK 17
        uses: actions/setup-java@dded0888837ed1f317902acf8a20df0ad188d165 #v5.0.0
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install and verify uyuni-java-parent
        run: >
          mvn -B --no-transfer-progress --file java/uyuni-java-parent/pom.xml --non-recursive install checkstyle:check

      - name: Install and verify uyuni-java-common
        run: >
          mvn -B --no-transfer-progress --file java/uyuni-java-common/pom.xml install checkstyle:check

      - name: Install and verify coco-attestation
        run: >
          mvn -B --no-transfer-progress --file java/coco-attestation/pom.xml install checkstyle:check
