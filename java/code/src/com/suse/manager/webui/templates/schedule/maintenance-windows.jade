include /templates/common.jade

#maintenance-schedules

script(type='text/javascript').
    window.csrfToken = "#{csrf_token}";
    window.timezone = "#{h.renderTimezone()}";
    window.localTime = "#{h.renderLocalTime()}";

+userPreferences

script(type='text/javascript').
    spaImportReactPage('maintenance/maintenance-schedules')
        .then(function (module) { module.renderer('maintenance-schedules') });
