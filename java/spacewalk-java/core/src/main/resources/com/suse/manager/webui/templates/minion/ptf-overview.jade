include ./minion-header.jade

#ptf-overview

script(type='text/javascript').
    window.csrfToken = "#{csrf_token}";
    window.serverId = "#{server.id}";

script(type='text/javascript').
    spaImportReactPage('minion/ptf/ptf-overview')
        .then(function(module) { module.renderer('ptf-overview') });
