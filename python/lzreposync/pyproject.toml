[build-system]
requires = ["setuptools", "setuptools-scm"]
bild-backend = "setuptools.build_meta"

[project]
name = "lzreposync"
version = "0.1"
dependencies = [
     "memory_profiler",
     "pytest",
     "requests"
]

[project.scripts]
lzreposync = "lzreposync:main"
