include /templates/common.jade

#image-sync

script(type='text/javascript').
    window.csrfToken = "#{csrf_token}";
    window.isAdmin = #{is_admin};

+userPreferences

script(type='text/javascript').
    spaImportReactPage('images/image-sync')
        .then(function(module) { module.renderer('image-sync') });
