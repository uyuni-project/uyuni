#!BuildTag: uyuni/proxy:latest
#!BuildTag: uyuni/proxy:%PKG_VERSION%
#!BuildTag: uyuni/proxy:%PKG_VERSION%-build%RELEASE%
apiVersion: v2
name: proxy
description: Uyuni proxy containers.
type: application
home: https://www.uyuni-project.org/
version: 0.0.0 # Only increase the patch number when changing, the rest is replaced when pushing to OBS
appVersion: "%PKG_VERSION%"
